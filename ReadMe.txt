Remember how we had to:
git init to set up a git tracking repo
touch XXXX.txt to add a file
git add to add a file to the staging environment ready for when we want to committ
i then used Nano to edit the file to write this
i will then use git commit to commit the file

